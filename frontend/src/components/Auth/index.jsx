/**
 * Responsabilidade (components/Auth):
 * Componentes de interface focado única e exclusivamente na feature de Autenticação.
 * Segue o padrão Feature-Sliced Design (ou feature modules).
 * Ex: Formulários de login, botões OAuth, modais de registro.
 */
