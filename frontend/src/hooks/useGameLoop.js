/**
 * Responsabilidade (hooks):
 * Custom hooks do React isolam lógicas de estado e side-effects (useEffect) complexos para fora da tela.
 * Ex: useGameLoop, useUpgrades, useAuth.
 * Isso garante que os componentes fiquem "limpos" (clean code), lidando estritamente com renderização.
 */
