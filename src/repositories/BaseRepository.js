/**
 * Responsabilidade (src/repositories):
 * Camada de acesso direto ao banco de dados.
 * Usam os Models ou ORMs para escrever ou buscar coisas no banco (SQL ou NoSQL) de forma encapsulada.
 * Protegem a aplicação (especialmente os Services) de lidarem com queries complexas, mantendo a responsabilidade do banco isolada aqui.
 */
