/**
 * Responsabilidade (src/services):
 * O coração da regra de negócio usando Orientação a Objetos.
 * Efetuam todas as validações pesadas, cálculos, e definem os fluxos do sistema.
 * Os Services são desacoplados do Express (não devem receber `req` ou `res`) e orquestram a inserção/obtenção de dados pedindo-os aos Repositories.
 */
