/**
 * Responsabilidade (src/controllers):
 * Recebem as requisições HTTP diretas vindas das Routes (req, res).
 * Extraem o corpo da requisição (body), parâmetros (params, query), e devolvem respostas HTTP e Status Codes pertinentes.
 * Eles NÃO processam lógica de negócio, apenas delegam as informações tratadas para os Services.
 */
